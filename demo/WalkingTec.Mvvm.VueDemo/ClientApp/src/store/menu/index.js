/**
 * 菜单和路由配置
 */

import data from "./data";

export const state = {
    items: data()
};

const getters = {};
const mutations = {};

const actions = {};

const store = {
    state,
    actions,
    getters,
    mutations
};

export default store;
