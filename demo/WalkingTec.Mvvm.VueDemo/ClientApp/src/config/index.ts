/**
 * 配置文件
 */
const config = {
    serverHost: "/wtm", // /api
    headApi: "/api",
    headerApi: "/api",
    // token 名称
    tokenKey: "token",
    userInfo: "userinfo",
    // debugger调试
    development: true
};
export default config;
