@model WalkingTec.Mvvm.Demo.ViewModels.SchoolVMs.SchoolListVM
<wt:tab>
  <wt:tabheaders>
    <wt:tabheader title="a" />
    <wt:tabheader title="b" />
  </wt:tabheaders>
  <wt:tabcontents>
    <wt:tabcontent>
            <wt:treecontainer items="CityTree" id-field="Searcher.CityId" height="500">
                <wt:searchpanel vm="@Model" reset-btn="true" old-post="true">
                    <wt:row items-per-row="ItemsPerRowEnum.Three">
                        <wt:textbox field="Searcher.SchoolCode" />
                        <wt:textbox field="Searcher.SchoolName" />
                        <wt:combobox field="Searcher.test" empty-text="全部" Empty-text="aaa" />
                    </wt:row>
                </wt:searchpanel>
                <wt:quote>当前时间： @DateTime.Now</wt:quote>
                <wt:grid vm="@Model" url="/School/Search" />
            </wt:treecontainer>

        </wt:tabcontent>
    <wt:tabcontent>
      abc
    </wt:tabcontent>
  </wt:tabcontents>
</wt:tab>
<script>
    function download(ids, datas) {
    }
     // $("#@Model.ViewDivId").parent().removeClass("donotuse_pdiv");

</script>
